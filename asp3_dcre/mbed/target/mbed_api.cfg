/*
 *  TOPPERS PROJECT Home Network Working Group Software
 * 
 *  Copyright (C) 2018 Cores Co., Ltd. Japan
 * 
 *  上記著作権者は，以下の(1)～(4)の条件を満たす場合に限り，本ソフトウェ
 *  ア（本ソフトウェアを改変したものを含む．以下同じ）を使用・複製・改
 *  変・再配布（以下，利用と呼ぶ）することを無償で許諾する．
 *  (1) 本ソフトウェアをソースコードの形で利用する場合には，上記の著作
 *      権表示，この利用条件および下記の無保証規定が，そのままの形でソー
 *      スコード中に含まれていること．
 *  (2) 本ソフトウェアを，ライブラリ形式など，他のソフトウェア開発に使
 *      用できる形で再配布する場合には，再配布に伴うドキュメント（利用
 *      者マニュアルなど）に，上記の著作権表示，この利用条件および下記
 *      の無保証規定を掲載すること．
 *  (3) 本ソフトウェアを，機器に組み込むなど，他のソフトウェア開発に使
 *      用できない形で再配布する場合には，次のいずれかの条件を満たすこ
 *      と．
 *    (a) 再配布に伴うドキュメント（利用者マニュアルなど）に，上記の著
 *        作権表示，この利用条件および下記の無保証規定を掲載すること．
 *    (b) 再配布の形態を，別に定める方法によって，TOPPERSプロジェクトに
 *        報告すること．
 *  (4) 本ソフトウェアの利用により直接的または間接的に生じるいかなる損
 *      害からも，上記著作権者およびTOPPERSプロジェクトを免責すること．
 *      また，本ソフトウェアのユーザまたはエンドユーザからのいかなる理
 *      由に基づく請求からも，上記著作権者およびTOPPERSプロジェクトを
 *      免責すること．
 * 
 *  本ソフトウェアは，無保証で提供されているものである．上記著作権者お
 *  よびTOPPERSプロジェクトは，本ソフトウェアに関して，特定の使用目的
 *  に対する適合性も含めて，いかなる保証も行わない．また，本ソフトウェ
 *  アの利用により直接的または間接的に生じたいかなる損害に関しても，そ
 *  の責任を負わない．
 * 
 *  @(#) $Id$
 */

/*
 *  gpio_apiのシステムコンフィギュレーションファイル
 */
#include "shellif.h"
#include "gpio_api.h"

/* CSI0 */

DEF_INH(INT_SCI0_RXI, { TA_HLNG, sci0_rxi_handler });
CFG_INT(INT_SCI0_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI0_TXI, { TA_HLNG, sci0_txi_handler });
CFG_INT(INT_SCI0_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI0_TEI, { TA_HLNG, sci0_tei_handler });
CFG_INT(INT_SCI0_TEI, { TA_NULL, -1 });

/* CSI1 */

DEF_INH(INT_SCI1_RXI, { TA_HLNG, sci1_rxi_handler });
CFG_INT(INT_SCI1_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI1_TXI, { TA_HLNG, sci1_txi_handler });
CFG_INT(INT_SCI1_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI1_TEI, { TA_HLNG, sci1_tei_handler });
CFG_INT(INT_SCI1_TEI, { TA_NULL, -1 });

/* CSI2 */

DEF_INH(INT_SCI2_RXI, { TA_HLNG, sci2_rxi_handler });
CFG_INT(INT_SCI2_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI2_TXI, { TA_HLNG, sci2_txi_handler });
CFG_INT(INT_SCI2_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI2_TEI, { TA_HLNG, sci2_tei_handler });
CFG_INT(INT_SCI2_TEI, { TA_NULL, -1 });

/* CSI3 */

DEF_INH(INT_SCI3_RXI, { TA_HLNG, sci3_rxi_handler });
CFG_INT(INT_SCI3_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI3_TXI, { TA_HLNG, sci3_txi_handler });
CFG_INT(INT_SCI3_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI3_TEI, { TA_HLNG, sci3_tei_handler });
CFG_INT(INT_SCI3_TEI, { TA_NULL, -1 });

/* CSI4 */

DEF_INH(INT_SCI4_RXI, { TA_HLNG, sci4_rxi_handler });
CFG_INT(INT_SCI4_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI4_TXI, { TA_HLNG, sci4_txi_handler });
CFG_INT(INT_SCI4_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI4_TEI, { TA_HLNG, sci4_tei_handler });
CFG_INT(INT_SCI4_TEI, { TA_NULL, -1 });

/* CSI5 */

DEF_INH(INT_SCI5_RXI, { TA_HLNG, sci5_rxi_handler });
CFG_INT(INT_SCI5_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI5_TXI, { TA_HLNG, sci5_txi_handler });
CFG_INT(INT_SCI5_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI5_TEI, { TA_HLNG, sci5_tei_handler });
CFG_INT(INT_SCI5_TEI, { TA_NULL, -1 });

/* CSI6 */

DEF_INH(INT_SCI6_RXI, { TA_HLNG, sci6_rxi_handler });
CFG_INT(INT_SCI6_RXI, { TA_NULL, -1 });

DEF_INH(INT_SCI6_TXI, { TA_HLNG, sci6_txi_handler });
CFG_INT(INT_SCI6_TXI, { TA_NULL, -1 });

DEF_INH(INT_SCI6_TEI, { TA_HLNG, sci6_tei_handler });
CFG_INT(INT_SCI6_TEI, { TA_NULL, -1 });

